import {Inter} from 'next/font/google';

export const inter = Inter({subsets: ['latin']});
export const lusitana = {
    className: 'lusitana-font-class', // Replace with the actual class name or object
    // Other properties if any
};